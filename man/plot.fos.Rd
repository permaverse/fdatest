% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fos.R
\name{plot.fos}
\alias{plot.fos}
\alias{autoplot.fos}
\alias{plot.IWT1}
\title{Plot for Functional One-Sample Test Results}
\usage{
\method{autoplot}{fos}(
  object,
  xrange = c(0, 1),
  alpha1 = 0.05,
  alpha2 = 0.01,
  ylabel = "Functional Data",
  title = NULL,
  linewidth = 0.5,
  ...
)

\method{plot}{fos}(
  x,
  xrange = c(0, 1),
  alpha1 = 0.05,
  alpha2 = 0.01,
  ylabel = "Functional Data",
  title = NULL,
  linewidth = 0.5,
  ...
)

\method{plot}{IWT1}(
  x,
  xrange = c(0, 1),
  alpha1 = 0.05,
  alpha2 = 0.01,
  ylab = "Functional Data",
  main = NULL,
  lwd = 1,
  col = 1,
  ylim = NULL,
  type = "l",
  ...
)
}
\arguments{
\item{object, x}{An object of class \code{fos}, usually a result of a call to
\code{\link[=functional_one_sample_test]{functional_one_sample_test()}} or \code{\link[=iwt1]{iwt1()}}.}

\item{xrange}{A length-2 numeric vector specifying the range of the x-axis
for the plots. Defaults to \code{c(0, 1)}. This should match the domain of the
functional data.}

\item{alpha1}{A numeric value specifying the first level of significance used
to select and display significant effects. Defaults to \code{alpha1 = 0.05}.}

\item{alpha2}{A numeric value specifying the second level of significance
used to select and display significant effects. Defaults to \code{alpha2 = 0.01}.}

\item{ylabel}{A string specifying the label of the y-axis of the functional
data plot. Defaults to \code{"Functional Data"}.}

\item{title}{A string specifying the title of the plots. Defaults to \code{NULL}
in which case no title is displayed.}

\item{linewidth}{A numeric value specifying the width of the line for the
functional data plot. Note that the line width for the adjusted p-value
plot will be twice this value. Defaults to \code{linewidth = 0.5}.}

\item{...}{Other arguments passed to specific methods. Not used in this
function.}

\item{ylab}{Label of the y-axis (legacy alias for \code{ylabel}). Defaults to
\code{"Functional Data"}.}

\item{main}{Plot title (legacy alias for \code{title}). Defaults to \code{NULL}.}

\item{lwd}{Line width (legacy alias for \code{linewidth}; divided by 2 for
ggplot2 scaling). Defaults to \code{1}.}

\item{col, ylim, type}{Ignored; retained for backward compatibility only.}
}
\value{
The \code{autoplot.fos()} function creates a ggplot object that displays
the functional data (with the null mean function \code{mu} overlaid as a dashed
reference line) and the adjusted p-values. The significant intervals at
levels \code{alpha1} and \code{alpha2} are highlighted in both panels. The
\code{plot.fos()} function is a wrapper around \code{autoplot.fos()} that prints the
plot directly.
}
\description{
The \code{S3} methods \code{autoplot.fos()} and \code{plot.fos()} are methods for plotting
results of functional one-sample tests. They visualize the functional data
and the adjusted p-values obtained from the testing procedures for testing
the center of symmetry of a functional population. The plots highlight
significant effects at two levels of significance, \code{alpha1} and \code{alpha2},
using shaded areas.
}
\examples{
# Performing the IWT for one population
IWT_result <- functional_one_sample_test(
  NASAtemp$paris, mu = 4, n_perm = 10L
)

# Plotting the results
plot(IWT_result, xrange = c(0, 12), title = "Paris temperatures")

# Selecting the significant components at 5\% level
which(IWT_result$adjusted_pvalues < 0.05)
}
\references{
Abramowicz, K., Pini, A., Schelin, L., Stamm, A., & Vantini, S. (2022).
“Domain selection and familywise error rate for functional data: A unified
framework. \emph{Biometrics} 79(2), 1119-1132.

Pini, A., & Vantini, S. (2017). Interval-wise testing for functional data.
\emph{Journal of Nonparametric Statistics}, 29(2), 407-424.

Pini, A., Vantini, S., Colosimo, B. M., & Grasso, M. (2018). Domain‐selective
functional analysis of variance for supervised statistical profile monitoring
of signal data. \emph{Journal of the Royal Statistical Society: Series C
(Applied Statistics)} 67(1), 55-81.

Abramowicz, K., Hager, C. K., Pini, A., Schelin, L., Sjostedt de Luna, S., &
Vantini, S. (2018). Nonparametric inference for functional‐on‐scalar linear
models applied to knee kinematic hop data after injury of the anterior
cruciate ligament. \emph{Scandinavian Journal of Statistics} 45(4),
1036-1061.
}
\seealso{
\code{\link[=IWTimage]{IWTimage()}} for the plot of p-value heatmaps (for IWT).
}
